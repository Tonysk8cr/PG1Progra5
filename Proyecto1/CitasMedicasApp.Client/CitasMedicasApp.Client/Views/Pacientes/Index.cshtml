@model List<AppCitasMedicas.DTO.Response.Pacientes.PacientesResponse>

<h2>Lista de Pacientes</h2>

<a asp-action="Crear" class="btn btn-success mb-3">
    Agregar Paciente
</a>

<table class="table table-striped">
    <thead class="table-dark">
        <tr>
            <th>Nombre</th>
            <th>Edad</th>
            <th>Telefono</th>
            <th>Email</th>
            <th>Acciones</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td>@item.Nombre</td>
                <td>@item.Edad</td>
                <td>@item.Telefono</td>
                <td>@item.Email</td>
                <td>
                    <a asp-action="Editar"
                       asp-route-id="@item.PacienteId"
                       class="btn btn-warning btn-sm">
                        Editar
                    </a>

                    <a asp-action="Eliminar"
                       asp-route-id="@item.PacienteId"
                       class="btn btn-danger btn-sm"
                       onclick="return confirm('¿Seguro que desea eliminar este paciente?');">
                        Eliminar
                    </a>
                </td>
            </tr>
        }
    </tbody>
</table>